<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/styles/image.css">
</head>
<body>
  <h1></h1>
  <div class="image">
    <div class="gallery-item">
    <img src="/images<%= image.filename %>" alt="<%= image.id%>" id="<%= image.id%>" data-api-url="/image/<%= image.id %>"/>
  </div>
</body>
</html>